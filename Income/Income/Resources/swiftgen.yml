coredata:
  inputs: Incomes.xcdatamodeld
  outputs:
    templateName: swift4
    output: CoreData.swift

strings:
  inputs: Base.lproj/Localizable.strings
  outputs:
    templateName: structured-swift4
    output: Strings.swift
    params:
      enumName: Localizable
  fallback: Base
